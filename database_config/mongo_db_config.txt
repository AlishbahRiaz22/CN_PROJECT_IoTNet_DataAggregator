# --- MongoDB Connection Details for Node-RED ---
#
# These are the settings configured in the 'mongodb' node within the flows.json file.

# Server Hostname/IP:
HOST = 127.0.0.1 (or localhost)

# Default Port (Standard MongoDB):
PORT = 27017

# Database Name (Used in Node-RED configuration):
DATABASE_NAME = campusdb

# Collection Name (Used in Node-RED insert operation):
COLLECTION_NAME = sensor_logs

